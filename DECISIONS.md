# 決定と保留

最終更新: 2026-08-31

このリポジトリ（`tonkura-blog-astro`）は、Next.js 版 `~/dev/tonkura-blog` を
Astro に移し替えたもの。ここには「**なぜそう移行したか**」と「**切替時に決めること**」を残す。
何をしたかは git 履歴にある。

---

## 全体の位置づけ

- **旧サイト（Next.js / tonkura.blog）は無傷で本番稼働中。** この Astro 版は
  別リポジトリ・別フォルダで、まだデプロイしていない。いつでも捨てられる。
- 移行の動機は、コンテンツ配信中心のサイトに対して Next の作りが重かったこと、
  および 3Dビューアをどのみち作り直す予定だったこと。
- **コンテンツは無改変が原則。** 変えたのは配置と、下記の機械的変換だけ。

## なぜこう作ったか（非自明な判断）

### コンテンツはすべてリポジトリ内に取り込んだ
`src/content/{blog,wiki,database,neurology}`。当初 blog/database は旧リポジトリを
外部参照していたが、**デプロイ先に旧リポジトリは無い**ので自己完結させた。
→ **旧リポジトリとで二重管理になっている。切替時に源を一本化すること（下記・未決）。**

### wiki は `.mdx` → `.md` にした
中身は純粋な Markdown で JSX を使っていない。`.mdx` のままだと MDX が
`<https://…>` 自動リンクを JSX と誤認してビルドが落ちる。**拡張子を変えただけ、内容は無改変。**

### 神経ノートの画像は public 配信、壊れリンクは 404 許容
- 相対パス画像が同居し、かつ**原コンテンツに壊れリンクが混在**する。Astro の
  画像最適化は存在しない画像でビルドを止めるため、画像を `public/neurology-media/` へ
  移し、md の画像パスを絶対パスに書き換えた（`src/content/neurology` のコピーを機械変換）。
- これで「最適化せず静的配信・壊れは 404」という旧レンダラ相当の寛容な挙動になる。

### 神経ノートの Obsidian 記法を変換した
`![[...]]`（埋め込み 64）→ `<img>`、`[[...]]`（wikilink 172）→ ノートへのリンク、に
機械変換した。画像名・ノート名の解決はインデックス照合。**曖昧・不在は 404/素通し（許容）。**

### デザイン：トップと個別ページで方針が違う
- **トップ**はエディトリアル・インデックス（大きなタイポの一覧、モノクロ、引き算）。
- **個別ページ**は旧サイトの `prose-slate` に寄せた（0.95rem、h1 1.75/h2 1.5/h3 1.25、
  暗いコードブロック #1a1b26、表 0.85rem、max-w-3xl）。
- **リンクは全ページ通してモノクロ**（青は使わない。本人の明確な希望）。
  デザインは `src/styles/tokens.css` と `prose.css`、コンポーネントに集約。ここを直せば全ページに効く。

### 検索除外（noindex）で立てている
`robots.txt` の Disallow と各ページの `<meta name="robots" content="noindex,nofollow">`。
**「まだ見つかってほしくない」という方針による。公開時に外す（下記・未決）。**

### 計算機は React 島
NIHSS / MMSE / HDS-R / IgG を `@astrojs/react` の島（`client:load`）として移植。
依存は react とローカル型のみ、PHI 入力なし。**ボタン操作の実挙動はブラウザ未確認。**

### ビューア依存ページはプレースホルダ
アトラス3種・ブロードマン・神経筋支配は「別プロジェクトで準備中」の
プレースホルダ（`src/layouts/Placeholder.astro`）。3Dビューアは別途作り直す。

---

## 疑問駆動パイプライン（2026-08-21 着手）

GitHub Issue に投げた疑問を Claude Code Action が調査・執筆し、記事の下書きとして PR で返す。
人は PR の差分を見て merge する。設計の元は `~/dev/pipeline-design.md`。

### なぜこのリポジトリに載せたか

指示書は「ブログリポジトリ1つに集約」を前提にしていたが、着手時点で**この Astro 版は
GitHub にリモートを持っていなかった**（ローカル2コミットのみ）。Phase 0 の
`/install-github-app` を走らせる先が無い状態だったため、まず public リポジトリとして
push した。これは上の未決 #1「コンテンツの源を一本化する」を **Astro 側に決めた**ことを意味する。
旧 Next.js 版 `tonkura-blog` は本番稼働のまま凍結扱いとする。

### なぜ `blog` ではなく `imaging` という別コレクションか

既存 `blog` の6本は出典リストを持たない個人ノートで、`sources.min(2)` を課すと全部落ちる。
厳しさの違うものを同居させると「出典ゼロを CI で落とす」というこの仕組みの要が成立しない。
指示書の案は `posts` だったが、`blog` と `posts` が並ぶのは読者にも次のセッションにも紛らわしいので
`/neurology` の隣に置く `imaging` にした。**画像解析に閉じない記事を書きたくなったら改名が要る**
（コレクション名・ディレクトリ名・`src/pages/imaging/` の3か所）。

### 指示書から変えた点と、その理由

- **`src/content/config.ts` は作らない。** Astro 7 は `src/content.config.ts` を先に探し、
  それが既に存在する（`astro/dist/content/utils.js:556`）。指示書のパスは Astro 4 時代のレガシー。
- **`related` の実在チェックは `reference()` 任せにしない。** `reference()` はスキーマ検証の
  時点では形しか見ず、存在しない id は `console.warn` を出して `undefined` を返すだけで
  ビルドは緑のまま通る（`runtime.js:129`）。`src/lib/imaging.ts` の `resolveRelated()` で明示的に throw する。
- **`sources` に「一次資料1件以上」を `.refine()` で足した。** 指示書では
  「一次資料を最低1件当たれ」がプロンプト頼みで、確認のためにもう一度 Claude を呼ぶ設計だった。
  zod で落とせるものを機械に任せる。
- **下書きは CI ではビルドに含める。** `INCLUDE_DRAFTS=1`（`npm run build:ci`）。
  PR の中身は必ず `draft: true` なので、本番と同じ条件でビルドすると下書きページが一度も
  描画されず、`related` の壊れリンクや Markdown の破綻を検証がすり抜ける。
- **`fetch-depth: 0` は使わない。** 履歴を全部引く指定であって、ファイルの取得範囲とは無関係。
  既定の shallow clone でも HEAD の全ファイルは揃う。この repo は画像で 170MB 超あり、full history は無駄。
- **記事ファイルは `{Issue番号}-{英語スラッグ}.md` のフラット配置。** 指示書は
  `{category}/{Issue番号}-{スラッグ}` を推していたが、その理由（URL の安定）を満たすなら
  category をパスに入れてはいけない。分類し直したときに URL と `related` の両方が壊れる。
- **textlint の `max-kanji-continuous-len` を無効化した。** 「安静時機能的結合」を弾く。
  この分野の日本語は長い漢字熟語が普通で、ルールが語彙をすべて誤検出する。
- **`no-mix-dearu-desumasu` は `preferInBody` で固定した。** 既定は文書内の多数決で
  向きが決まり、記事によって「ですます調にせよ」と言い出す。

### やらなかったこと

- **`astro check` を CI に入れていない。** 既存ファイルに型エラーが12件残っている
  （`atlas.astro` 4 / `calculator.ts` 3 / `gallery/index.astro` 3 / `neurology.ts` 2）。
  今入れると初日から常時赤になり、CI が形骸化する。`atlas.astro` の4件は NiiVue の型で、
  ブラウザ実挙動が未確認のまま直すと壊しかねないので手を付けていない。
  `npm run check` で見られる。緑にしてからワークフローに足すこと。
- **お手本記事を書いていない。** 指示書の Phase 1-2。出典を実際に当たって書く必要があり、
  筆者の声で書くものなので Claude が代筆すべきものではない。`000-template.md` は
  スキーマと描画を確かめるための骨組みで、記事ではない。

### Phase 0（`/install-github-app`）は完了した（2026-08-21）

main に `.github/workflows/claude.yml`（Issue / PR コメントの `@claude` で起動）と
`claude-code-review.yml`（PR ごとの自動レビュー）があり、`CLAUDE_CODE_OAUTH_TOKEN` も登録済み。

**この過程で踏んだ落とし穴を 2 つ残す。**

- **このコマンドは cwd の git リポジトリを対象にする。** 最初 `~/dev`（git 管理外）で開いた
  セッションから実行して失敗した。必ず対象リポジトリのディレクトリで `claude` を起動すること。
- **インストーラはワークフローをブランチに push するだけで、PR を開かない。**
  `add-claude-github-actions-<数字>` というブランチができ、"GitHub App installed!" と表示される
  ので済んだように見えるが、main には何も無く `@claude` は無反応のままになる。
  実際 2026-08-21 はこの状態で数時間放置していた。`gh api /repos/{owner}/{repo}/actions/workflows`
  で main 上の active なワークフローを見れば確実に判る。今回は main に fast-forward で取り込んだ。

### 未決

1. **`claude-code-review.yml` を疑問駆動パイプラインに噛ませるか。** 既定のままだと
   **すべての PR** が自動レビュー対象になる。パイプラインが吐く記事の下書き PR は中身が
   Markdown で、コードレビュー用のプロンプトが役に立つとは限らない。`paths` で絞るか、
   `src/content/imaging/**` には別プロンプトを当てるか、未決。
2. **`claude.yml` は既定のプロンプトのまま。** Issue の疑問 → `imaging` 記事の下書き → PR、
   という本来の流れは入っていない。Phase 2 の作業。
3. **お手本記事が未執筆。** 本人と対話しながら1本書く方針で合意済み（2026-08-21）。
   出典を実際に当たり、本人の声で書く。Claude の代筆にはしない。
   これが Phase 2 のスキルを詰めるときの基準になるので、Phase 2 より先にやる。
4. **AI 生成であることを記事に明記するか。** 「Claude が下書きし、筆者が検証・加筆した」の
   一行を入れるかどうか。public な医学系ブログなので入れる方に寄せたいが、未決。
5. **図をどうするか。** 方法論の解説は図がないと辛い。Mermaid を Astro で
   レンダリングできるようにすれば Claude が図まで書ける。
6. **過去の会話の棚卸し。** 埋もれた疑問をまとめて Issue 化する初期移行をやるか。

## Markdown ファイル名を英小文字スラッグに統一した（2026-08-22）

5コレクションで命名が5通りに割れていた（日付前置き / `q-` 前置き / Issue番号 / 日本語 / 大文字・空白）。
これを `[a-z0-9-]+.md` に統一した。**規約そのものは CLAUDE.md にある。ここには理由と経緯を残す。**

### 揃えた動機は美観ではない。実害が出ていた

**Astro はコレクションの id をスラッグ化する。** 空白はハイフンになり、括弧は消え、大文字は小文字になる。
`Lower Motor Neuron Syndromes.md` の URL は `/neurology/.../lower-motor-neuron-syndromes` である。
ところが Obsidian の `[[...]]` から機械変換した本文中のリンクは、変換時の名前のまま埋まっていた。

照合したところ **本文内リンク 147 本のうち 43 本（29%）が 404** だった。内訳は
全角括弧 `（）` が URL 化で消える 31、空白 6、半角括弧 `(` で Markdown リンクが途切れる 3、
Notion 由来の `Untitled` 2、参照先の不在 1。

**このうち 42 本は原コンテンツ由来ではなく、命名とスラッグ化規則の食い違いで生じていた。**
「壊れリンクは 404 許容」と上に書いたのは、原因の切り分けが足りていなかった。
改名とリンク書き換えを同時に行い、**147/147 が到達する状態にした。**

### 英訳の典拠は `database` 自身に置いた

`neurology/高次脳機能` の 225 本は日本語のみのファイル名で、英訳が要った。
勝手に訳さず、**`database`（高次脳機能症候の事典）が既に持っている 90 件の対訳を典拠にした。**
これで筆者自身の語彙選択と揃う。

- 失読失書 → `alexia-agraphia`（`alexia-with-agraphia` ではない）
- 他人の手徴候 → `alien-hand-syndrome`（`alien-hand-sign` ではない）
- 把握現象 → `grasping-reflex`、構成障害 → `constructional-disorder`、語義失語 → `gogi-aphasia`

`database` に無い神経病理の用語は PubMed で使用実績を確認した（`argyrophilic grain` 339件、
`siderophage` 193件、`gitter cell` 55件、`motor impersistence` 79件）。
**原文の綴り誤りは正した。** `Charot` → `charcot`、`parapresis` → `paraparesis`。

### 改名は表示名を破壊する設計だった。frontmatter へ退避して回避した

`src/pages/neurology/[...slug].astro` と `src/lib/neurology.ts` は、
**ページの H1 もツリーのラベルも `entry.id`（＝パス）から作っていた。**
英語スラッグに改名すると 319 本の見出しが全部英語になる。改名前に退避させた。

- **各 .md の frontmatter に `title:`**（元の日本語名）。スキーマに `title` を追加した
- **各 `order.json` に `label:`**（ディレクトリの日本語名）
- `TreeNode` に `label` を足し、描画3ファイルを `label` 優先にした

**この構造は維持すること。** ファイル名は URL、`title`/`label` は表示名、と役割が分かれている。
ファイル名から表示名を作るコードを書き戻すと、また日本語が消える。

**踏んだ罠を1つ残す。** Astro は `index.md` をディレクトリ自身の id に割り当てる。
`examination/index.md` に `title: "index"` を入れたらトップ階層の見出しが `index` に潰れた。
`index.md` の `title` は親ディレクトリの表示名にすること。

### ファイル名に Issue 番号を入れるのをやめた（2026-08-22）

当初 `imaging` は `{Issue番号}-{slug}.md` だった。これを外して `{slug}.md` にした。

**Issue 番号は記事の依存関係を表せない。** GitHub は Issue と PR で同じ連番を作成順に振る。
実例として、MRI の信号生成は BOLD の**上流**にあたる前提知識だが、BOLD が Issue #3、
MRI 基礎が Issue #5 になった（#1・#2・#4 は PR が消費している）。
**若い番号を後から取ることはできない。** 記事を書くたびにこのねじれが起きる。

読む順を別の連番（`01-`, `02-`）で表す案は採らなかった。間に記事を挟むたびに
振り直しが要り、URL が壊れる。**category をパスから外したのと同じ罠である。**

- **Issue との対応は frontmatter の `fromIssue` だけが持つ。** ページ下部に Issue への
  リンクが出る。**スキーマで必須にした**ので、Issue を通さない記事は CI で落ちる
  （実際に `template.md` が落ちることを確認した）
- **読む順は `related` と本文中の導線で示す。** 番号では示さない
- 既存記事の分割で生まれた記事も、遡って Issue を立てる（MRI 基礎は Issue #5）

### ただし並び順番号は戻した。URL から落とすことで両立させた

番号を全部外すと、**リポジトリのファイル一覧で記事が意図した順に並ばない**。
VSCode のエクスプローラで上流の記事が下に来るのは実用上わかりにくい。

`content.config.ts` の `generateId` で**先頭の `\d+[-_]` を id から落とした**。
Astro 7 の glob ローダーが `generateId` を受け取れる（`glob.d.ts:19`）。

    01-mri-signal-basics.md  →  /imaging/mri-signal-basics
    20250321-tau-pet.md      →  /blog/tau-pet

**これで「振り直すと URL が壊れる」という理由自体が消えた。** 番号はファイル名だけの
存在で、URL・`related`・本文中のリンクは一切参照しない。間に記事を挟むときは
番号を振り直してよい。

**番号は読む順であって Issue 番号ではない。** Issue との対応は `fromIssue` が持つ。
この2つを1つの数字で兼ねようとしたのが最初の誤りだった。

見本ファイルは `000-template.md` → `00-template.md`（桁を揃えた）。

### 元から壊れていて、今回直していないもの

- **`order.json` の実在しない参照。** `代謝性疾患`・`注意障害` は改名前の HEAD にも存在しない。
  将来書くときのために規約名（`metabolic-disease`・`attention-disorders`）へ変換して残した
- **画像 30 本の欠損**（68 本中）。原コンテンツ由来で、改名の前後で実在率は 38/68 のまま変わらない

### 積み残し

- **`public/neurology-media/` は日本語のまま。** 68 本の画像が絶対パスでここを指している。
  **URL に日本語が残る唯一の場所。** 揃えるなら画像の移動と参照の書き換えを同時に行うこと
- **`neurology/高次脳機能` と `database` の内容が重複している。** 対訳作業で判明した。
  失読失書・他人の手徴候・語義失語など 30 項目以上が両方にある。統合するか、
  どちらかを正とするかは未決
- **`neurology` の改名で URL が全部変わった。** 未デプロイ・noindex なので実害は無いが、
  公開後に同じことをすると外部リンクが切れる

## 文章の作り方（2026-08-23）

`imaging` の記事は Claude が下書きする。**その文体をどう詰めたかを残す。**
規約そのものは `CLAUDE.md` の「文体」「構成」「リズムと型」にある。ここには理由と、
**採らなかった案**を書く。

### 字数上限を撤廃した

当初 1500〜3000字を目安にしていた。BOLD の記事で「常磁性」「T2\*」「線形時不変系」を
定義せずに使っており、**MRI 物理が初心者という想定読者が辿れない状態**だった。
分量を守るために説明を省くと記事の目的が立たなくなるので、上限を外した。

### 「AI っぽさ」は実測してから決めた

「Claude が書くと AI 感が出る」という指摘を受けて Web を調べ、**一般に言われる指標を
そのまま採らずに、Claude の記事と筆者の文章を実測して比較した**（計測結果は git 履歴、
スクリプトはセッション限りなので残っていない）。

**空振りだった指標**（禁止する必要がなかったもの）:

- 中身のない形容詞（重要な・様々な・多くの）… Claude 0%、筆者 wiki 1%
- 機械的な接続詞（これにより・したがって）… Claude 0%、筆者 wiki 1%
- 過剰なヘッジ … Claude 1%、筆者 wiki 1%

**意図的に禁止しなかったもの:**

- **否定対比（「〜ではなく、〜である」）。** 英語圏で「AI の最大の手がかり」と名指しされるが、
  **実測では筆者の wiki が12%、Claude が6%で、筆者のほうが2倍使っていた。**
  禁止すると筆者の声から遠ざかる。次のセッションがこれを「AI の癖」として消しにかからないこと。
- **「体験談を入れる」「感情を込める」という一般的な助言。** `CLAUDE.md` の
  「体験談の捏造の禁止」「淡々と書く」と正面から衝突する。筆者が実際に手を動かしていない
  以上、体験談で埋めるのは捏造になる。

**実際に差が出た2点だけを規約にした:**

1. **文の長さが揃いすぎ。** Claude は 21〜35字に35%が集中し標準偏差 19.6、筆者 wiki は 38.5。
2. **用語定義の型が1つ。** `〜を…（English）と呼ぶ` が122文中10文で、うち
   「これを〜と呼ぶ」が4回近接していた。

### textlint の文長上限を 120 → 150 にした

**上限120字は筆者自身の文体より厳しく、それ自体がリズムを平板にしていた。**
筆者の wiki 1049文のうち 6.5% が120字を超える（最長307字）。
97パーセンタイルが148字なので150字にした。自然な範囲の97%を通し、極端な長文だけ捕まえる。

### 出典を番号引用に変えた（前の決定を覆した）

当初は「出典は frontmatter だけに持ち、**本文に引用マーカーを置かない**」と決めていた。
これだと**どの記述がどの出典に基づくか本文から辿れない**ため、論文と同じ番号方式にした。

- 本文に `[3](#ref-3)` と書く。**角括弧はページ側の CSS が付ける**ので、
  Markdown を生で読んでも `[3]` と見える形にしてある
- `sources` の並び順が番号であり、**本文での初出順に並べる**
- 出典の `<li>` に `id="ref-N"` を振って飛べるようにした

### 定型の見出しをやめた

`## 実務上の注意` のような、どの記事にも置ける見出しを使わない。
**記事は疑問への答えであって、書式の埋め合わせではない。**
各項目を `**〜は〜で決まらない。**` と太字の名詞止めで始める書き方も、
それ自体が定型の匂いの元だったのでやめた。

### 未達のまま残っていること

**文長の標準偏差は 21.9 で、筆者の 38.5 に届いていない。** 21〜35字への集中は
35%→26%（筆者23%）まで下げ、「〜と呼ぶ」は10文→0にしたが、上側が伸びていない
（71字以上が13%、筆者は23%。最長文は110字のまま）。
**上限を150字に上げても長い文は書かれなかった。** 「書いてよい」と許可するだけでは
足りないことが数字に出ている。規則の書き方を変えるか、この水準で許容するかは未決。

**改稿の途中で型を入れ替えただけの状態を一度作った。** 「〜と呼ぶ」を消したら
文末最頻が「）である」×7 に移った。**1つの型を禁止すると別の型に集中する。**
散らしたかを最頻文末の占有率で確認すること（現在 6.5%、筆者 wiki は 5.7%）。

## 切替（公開）時に決めること — 未決

1. **コンテンツの源を一本化する。** 今は旧リポジトリと Astro 側の二重。
   今後どちらで編集するか決め、他方を従にする（または旧を凍結）。
2. **noindex を外す。** `robots.txt` と Base.astro の meta。公開の意思が固まってから。
3. **デプロイ。** → **2026-08-31 に実施した**（下記「Vercel に載せた」）。
   残るのは `tonkura.blog` の向け替えで、これは #2 の noindex を外す判断と対になる。
4. **Sentry を入れるか。** 旧サイトは `@sentry/nextjs` で有効。Astro は `@sentry/astro`。
   今は未配線（DSN 設定が要る）。入れるなら切替時に。
5. **3Dビューアの別プロジェクト。** アトラス系はここでは扱わない。
   → **2026-08-23、筆者が `~/dev/3D_Brain` の生成物を `public/brain-viewer/` に置き、
   `anatomical-atlas.astro` をプレースホルダから iframe 埋め込みに差し替えた**（`d5865d8`）。
   **上の「ビューア依存ページはプレースホルダ」は、このページについては過去の記述になる。**
   ビューアの作成自体は別プロジェクトのまま。ここには生成物だけが入っている。
   → **2026-08-23 追記。埋め込んだ直後、ビューアは three.js を jsdelivr から
   読んでいた。**CDN を落として試すと**進捗 0% のまま無言で止まる**。読者からは
   「重いページ」と区別がつかない。three.js を `public/brain-viewer/vendor/three/`
   に同梱して解消した（r163・改変なし・MIT、1.5MB / gzip 約 300KB）。
   これで「全コンテンツが自己完結」が再び成り立つ。
   **`public/brain-viewer/` を手で編集しないこと。** 配る側は `~/dev/3D_Brain/viewer/`
   ひとつで、`~/dev/3D_Brain/tools/sync_viewer.sh` が撒く。
   → **2026-08-24 追記。ビューアを3度撒き直した**（`c6c1673` / `2107010` / `c59f5a3`）。
   スマートフォン幅への対応が入り、マクロ（脳葉・皮質下核など）のアトラスが加わって
   既定の表示がマクロになった。資産は 24MB（`brain.glb` 7.2MB / `brain_macro.glb` 6.3MB /
   `brain_arterial.glb` 3.7MB / 断面 5.4MB / three.js 1.5MB）。
   **`viewer.html` と資産は同時に撒くこと。** `c6c1673` は `viewer.html` だけを差し替えており、
   既定がマクロに変わったにもかかわらず `brain_macro.glb` と断面画像が無い。この状態では
   読み込みが完了しない。資産は次の `2107010` で入っている。

## Vercel に載せた（2026-08-31）

プロジェクトは `tonkura-blog-astro`（team `tonkuras-projects`、Hobby）。GitHub の
`tontonkurakura/tonkura-blog-astro` に接続してあり、main への push で本番ビルドが走る。
初回のビルドは 24 秒で、`astro build` が 455 ページを出力した（デプロイ全体では 35 秒）。
アダプタの追加や設定の変更は要らなかった。
本番 URL は <https://tonkura-blog-astro.vercel.app>。
**旧サイト（Vercel プロジェクト `tonkura-blog`、tonkura.blog）は触っていない。**

**Vercel Authentication が有効になっている**（`all_except_custom_domains`）。`.vercel.app`
の URL は Vercel にログインしていないと 302 で SSO に飛ぶので、スマートフォンで確認する
ときも一度ログインが要る。カスタムドメインを当てたぶんには効かない設定なので、
`tonkura.blog` を向けた瞬間に素通しになる。**向け替えは noindex の判断とセットにすること。**
検索避けは付けたまま（456 の HTML すべてに meta、`robots.txt` は `Disallow: /`）。

**下書きはデプロイされない。** 本番ビルドは `astro build` なので、`draft: true` の
`imaging` 3本は出力に含まれない。記事の見た目を実機で見たいときは `npm run dev` を使うか、
Preview 環境にだけ `INCLUDE_DRAFTS=1` を入れる。

**MCP の Vercel コネクタからはプロジェクトを作れなかった。** `create_git_project` が
403 forbidden を返す（権限はコネクタ側の制約で、アカウントの問題ではない）。ローカルの
`vercel` CLI は `tontonkurakura` として認証済みで、`vercel project add` →
`vercel link` → `vercel git connect` の順で通った。

**`vercel link` は `.env.local` に OIDC トークンを落とす。** `.gitignore` に `.vercel` と
`.env*` を足してある（`6a029c4`）。

## 旧サイトの URL をどう受けるか（2026-08-31）

`tonkura.blog` を新サイトへ向ける前に、旧サイトが今どう応答しているかを確かめた。

**`/neurology` は壊れている。** 一覧も個別ページも、すべてトップへ 307 で飛ぶ
（`/neurology`、`/neurology/view/Neuroanatomy/高次脳機能/診察/色彩認知障害` などで確認）。
319本のノートは現在の本番サイトから読めない。向け替えると、ここは復活する。

**逆に後退するページが4つある。** `/functional-atlas`、`/white-matter-atlas`、
`/neuromuscular`、`/database/brodmann` は旧サイトで 200 を返すが、こちらでは
まだプレースホルダである。向け替えは、この4ページを一時的に落とすことと引き換えになる。

`vercel.json` に転送を3本置いた。

| 旧 | 新 | 種別 |
|---|---|---|
| `/database/higher-brain-function/symptoms/*` | `/database/*` | 308 |
| `/neurology/view/*` | `/neurology` | 307 |
| `/daisetsu` | `/` | 308 |

`database` はカテゴリと id がそのままで、前置きの `higher-brain-function/symptoms/` が
落ちただけなので、ワイルドカード1本で吸収できる。`/daisetsu` は旧サイトでも
`router.push("/")` でトップへ飛ぶだけのページだった。

**`/neurology/view/*` を 307（一時）にしたのは、記事単位の対応表を後から作る余地を
残すためである。** 308 はブラウザが恒久的にキャッシュするので、後から個別の転送を
足しても効かなくなる。対応表そのものは `git show aa60c15 -M10%` の rename 情報から
319本中 302本まで機械的に取れる。ただし旧 URL は今もトップへ飛んでおり、検索エンジンから
見て既に生きていない。**いま 300 本の転送を書く価値は低いと判断した。**

**`/blog?tag=` と `/blog?page=` は転送していない。** クエリ文字列を見る転送には `has`
条件が要るうえ、新サイトのタグは `/blog/tags/` という別の形になっている。

### 切替の手順（決定済み、未実行）

2026-08-31 に方針を決めた。**実機確認を終えてから向け替える。noindex は同時に外す。**
`tonkura.blog` は現在インデックスされている（`site:` 検索でトップ、`/neuromuscular`、
neurology の記事がヒットする）ので、noindex を付けたまま向けると検索結果から消える。

順序を逆にしないこと。**先に noindex を外してデプロイし、次にドメインを移す。**
`.vercel.app` は SSO で守られていてクローラが入れないため、noindex を先に外しても
その間に拾われることはない。逆順にすると、noindex が付いた状態の `tonkura.blog` を
クローラが拾う時間ができる。

1. `src/layouts/Base.astro` の `noindex, nofollow` を消し、`public/robots.txt` を緩める
2. push してデプロイ（この時点ではまだ `.vercel.app` だけ）
3. `tonkura.blog` を新プロジェクトへ移す（旧プロジェクトから外し、新へ追加）
4. 転送3本と主要ページを実際の URL で確認する

## 計算機は Tailwind のクラスで書かれていた（2026-08-31）

実機で見て「ボタンがモダンでない」と指摘を受けた。原因は意匠の好みではない。
**4つの計算機に `className` が 523 個あるが、このプロジェクトに Tailwind は入っていない。**
移植元の Next.js 版は Tailwind を使っており、部品をそのまま持ってきたのでクラスだけが残った。
`bg-white` も効かないので素の HTML が出ていた。ダークモードが崩れずに見えていたのは、
その `bg-white` が効いていなかったからである。

**Tailwind を入れるのではなく、`src/styles/calculator.css` に組み直した。** 入れると
`bg-white` が効きはじめ、ダークモードで白いカードが出る。`text-blue-600` も蘇るので、
「リンクに青を使わない」という方針とも衝突する。tokens.css の変数で書き直すほうが、
足すものが少なくて済む。

意匠の決めごとは3つある。

- **選択済みのボタンは前景色を反転させる**（`--fg` の地に `--bg` の文字）。青は使わない。
- **重症度は `data-level`、基準を外れた行は `data-state` で表す。** クラス名に色を持たせない。
  段階は `none` / `mild` / `moderate` / `severe` / `critical` の5つで、色は CSS 側にある。
  判定の刻みは移植元のままで、`bg-blue-600` のような戻り値を段階名に置き換えただけである。
- **色は彩度を落とし、ライトとダークで別の値を持たせる。** 一覧の意匠と同じ作法にした。

ついでに `/anatomical-atlas` がナビにもトップページにも載っていなかったのを直した。
ページは存在するのに、URL を直接叩く以外の入口が無かった。

**確認したこと。** ヘッドレス Chrome で NIHSS・MMSE・IgG index を描画し、ライトとダークの
両方を見た（ダークは `data-theme="dark"` を注入した複製ページで確認）。`astro check` の
エラーは 12 件のままで、増えていない。**ボタンを押したときの挙動は確認していない。**

## トップページを更新履歴中心にした（2026-08-31）

「シンプルすぎる」という指摘を受けて3案を作り、更新履歴を主役にする案を採った。
セクション名は囲み（チップ）をやめ、8つを4列2段の文字組みにしてある。件数は出さない。

**日付を持たないコレクションがある。** これは今後もフィードに影響する。

| コレクション | 使える日付 |
|---|---|
| blog | `date`（2025-02〜03）。`lastmod` は全6本が 2026-07-21 で使えない |
| neurology | `last_edited`（2021〜2025）。`lastmod` は 261 本が 2026-07 |
| imaging | `pubDate`。ただし下書きは本番ビルドに出ない |
| wiki | **無し。** `updated` は 11 本すべて 2026-07-19 |
| database | **無し。** `lastmod` は 90 本すべて 2026-07 |

移行時に一括で入った値は、項目ごとの意味を持たない。**wiki と database をフィードに
入れないのはこのためである。** 入れると、その日付だけで9件が埋まる（実際に一度そうなった）。

いま出る最新は 2025.03.22 である。`imaging` の記事を公開すれば 2026 の日付が頭に来る。

### 最上部ではヘッダーを出さない（トップページのみ）

`Base.astro` に `revealHeaderOnScroll` を足した。付けたページでは、ヘッダーを
`position: fixed` にして画面の外へ退避させ、160px 下に降りたところで現す。他のページは
これまでどおり `sticky` のままで、最初から見えている。**トップだけ挙動が違う。**

`sticky` のまま隠すと、隠したぶんの空白が流れに残る。`fixed` にして流れから外すと、
見出しが画面の一番上から始まる。

**判定は交差監視（IntersectionObserver）で行う。** 本文の先頭に高さ 160px の目印を
絶対配置し、それが画面から外れたら現す。スクロールイベント＋`requestAnimationFrame` でも
書けるが、`ticking` が立ったままになる失敗があり得るうえ、上下の向きで条件を分ける必要が出る。

**踏んだ罠が2つある。**

- **`<script is:inline>` を Astro の式（`{cond && (...)}`）の中に置くと、中身が
  テンプレートリテラルの文字列として出力され、実行されない。** エラーも出ない。
  素のマークアップとして置き、対象の要素が無ければ早期 return させる形にした。
- **ヘッドレス Chrome のスクリーンショットでは、この挙動を検証できない。**
  交差監視が初回の1回しか発火せず（`in@0, out@520` で止まる）、上に戻したときの
  発火が観測できない。スクロールイベント版でも同じだった。描画フレームが継続的に
  生成されないためと考えられる。`puppeteer-core` で実ブラウザを操作して確認した
  （最上部→隠れ、下降→現れ、復帰→隠れ、を往復で確認）。

## 文章4種は入口だけ統一した（2026-08-31）

`wiki` / `neurology` / `blog` / `imaging` は読み手には同じ「文章」に見えるのに、入口が
4つあった。**コレクションは分けたまま、ナビと入口だけ Writing に束ねた。**

統合しなかったのは、4つで契約が違うためである。`wiki` は問いのグラフ（`depends_on`）、
`neurology` は 319 本の階層（`order.json` が表示順とラベルを持つ）、`blog` は日付順、
`imaging` は Issue 起点で `sources` 必須（欠けると CI が落ちる）。1つのコレクションへ
統合すると URL が全部変わり、この検証も壊れる。**得るものは入口の数だけで、失うものが多い。**

`/writing` が4つを日付順に並べ、種別で絞り込む（全 335 本）。絞り込みは表示の
切り替えだけで、全件が最初から DOM にある。ページ下部から各索引へ行ける。

### 日付の出どころを1つ訂正する

前の節で「wiki は日付を持たない」と書いたが、これは誤りだった。`updated`（11本すべて
2026-07-19）は移行時の一括値だが、**`sessions[].date` は勉強会が実際に開かれた日で、
項目ごとの実体がある**（10問すべて 2026-07-10、第14回）。フィードではこちらを使う。

`database` は依然として日付を持たない（`lastmod` が 90 本すべて 2026-07）。

### 同じ日のものが並ぶのを避ける

素直に日付順で採ると、**先頭10行が「2026.07.10 / Wiki」で埋まった**。10問が同じ回の
勉強会から出ているためで、データとしては正しいが、読み手には不具合に見える。
種別ごとに6本までとした。写真も同じ問題が出た（新しい3枚が同じ日の同じ桜だった）ので、
**同じ撮影日からは1枚だけ採る**。

### 写真に撮影日を入れた

`src/data/photos.yaml` の各項目に `date` を足した。EXIF の `DateTimeOriginal` から
62枚すべて取れた（2015〜2025）。**`DateTime` は編集日なので使わない**（実例: `DSC00072`
は撮影 2022-03-14、`DateTime` は 2025-02-23）。GPS は 1 枚も入っていない。

### `@types/node` と `@types/js-yaml` を入れた

`astro check` のエラーが **12 件から 7 件に減った**。`node:fs` と `js-yaml` の型が
無いことによる分が消えたためで、`gallery/index.astro` の既存エラーも含まれる。
CI に入れる条件（緑にする）に一歩近づいた。残り 7 件は `atlas.astro` の NiiVue まわりなど。

## ギャラリーは旧サイトの見せ方に戻した（2026-08-31）

移行時のギャラリーは CSS の段組みに説明文を添えただけのものだった。**旧サイト
（Next.js 版）の作りが気に入っているとのことで、そちらに寄せ直した。**

- **扉。** 画面いっぱいの1枚（`DSC02359-Enhanced-NR-Pano-Edit.jpg`）に `Ephemera` と
  `- Fragments of Consciousness -` を重ね、下向きの矢印を置く。右下に撮影地。
  この1枚は下の並びには出さない。
- **並び。** 3列に順繰りで配る（`index % 3`）。高さは向きで決め打ちで、旧サイトの
  `PHOTO_HEIGHTS` と同じ値を使う（縦 600 / 横 371 = 600÷黄金比 / 正方形 500）。
  地は純黒、角丸、白い薄縁と淡い光。指すと拡大して撮影地が下から出る。
- **拡大。** `<dialog>` の標準機能を使う。ESC と外側を押すと閉じる。
  旧サイトは React でフォーカストラップを自前で書いていたが、`showModal()` が同じことをする。

**写真のページだけ暗い配色で固定する。** `Base.astro` に `forceDark` を足した。付けると
`data-theme="dark"` が入り、読者の設定を当てるスクリプトが動かず、切り替えボタンも消える。
地はさらに沈めて純黒にしてある。あわせて本文の枠と余白を外す `bare` も足した。

**ヘッダーは旧サイトと違う。** 旧サイトは半透明のナビを扉に重ねていたが、こちらは
トップページと同じ `revealHeaderOnScroll` にした。扉には何も乗らず、降りると出てくる。

### 写真のメタデータは2か所に分けた

- **説明（撮影地）は `src/data/photos.yaml`。** 人が書く。
- **撮影日・機材・絞り・ISO は JPEG から読む**（`src/lib/photos.ts`、`exifr`）。機械が持つ。

**この日の途中で `photos.yaml` に `date` を書き込んだが、取り消した。** ギャラリーで
機材や絞りも要ることが分かり、EXIF を読む口をどのみち作ることになった。両方に日付を
置くと二重管理になる。**写真を足したときに書き足すのは説明だけでよい。**

**画像の縦横は EXIF に入っていない**（62枚とも `ExifImageWidth` が無い）。JPEG の
SOF マーカーから読んでいる。向きの判定に使う。

### `set:html` で入れた中身にはスコープが効かない

ピンのアイコンを `Fragment set:html` で入れたら、**`.place .pin { width: 1.15em }` が
当たらず、画面いっぱいに広がった。** Astro のスコープ付きスタイルは `data-astro-cid-*`
属性で効くが、`set:html` で流し込んだ要素にはこの属性が付かない。部品
（`PinIcon.astro`）に切り出して解決した。

## ギャラリーのカクつきは画像の原寸配信だった（2026-08-31）

指で追ったときに引っかかる、という指摘があった。**Astro 固有の挙動ではない。**
移行時に写真を `public/images/` へ置いたため、`astro:assets` の対象から外れ、
**原寸の JPEG が62枚そのまま配られていた**（合計 78MB、最大 4672×7008 の 13.9MB）。
4672×7008 のビットマップを 400px の枠に描いて `scale(1.05)` を掛ければ、そのたびに
再サンプリングが走る。旧サイトは Next.js の `<Image>` が縮小と WebP 変換をしていた
（`utils/photoUtils.ts` の `convertToWebp`）ので、この問題が出なかった。

`src/assets/photos/` へ移し、`getImage()` で2種類を作る。

| | 幅 | 実測 |
|---|---|---|
| 一覧のサムネイル | 900px | 平均 73KB |
| 拡大表示 | 2200px | 平均 225KB |

**最初に読む量は 78MB から 4.4MB になった。** 拡大は押した1枚だけを後から読む。
原寸は配らない。`will-change: transform` は指した瞬間だけ付ける（61枚に常時付けると
層が増えてメモリを食う）。

**代わりに、参照されない原寸が `dist/_astro` に出る（78MB）。** `import.meta.glob` で
読み込んだ画像は、`getImage()` にしか渡していなくてもビルド成果物に含まれる。
閲覧者は読まないので体感には響かないが、配布物は太る。消したければ、sharp を直に叩く
前処理を書いて画像を `src` の外に出すことになる。**いまは素直な作りを採った。**

**旧サイトの変換結果が `public/images/webp/` に119ファイル（23MB）残っていた。**
どこからも参照されていない。消した（git 履歴には残る）。

## 拡大表示を作り直した（2026-08-31）

撮影情報を画像の右に縦並びで置く。記号は付けず、左揃え。値の無い項目は行ごと消す。

**押した写真の位置から拡大先へ動かす**（FLIP）。先に一覧のサムネイル（もう読んである）を
出して動かし、大きいほうは読めた時点で差し替えるので、待ち時間が見えない。閉じるときは
逆再生してから閉じる。**ESC は `cancel` を横取りして同じ動きを通す**（既定のままだと
戻る動きを飛ばして即座に閉じる）。`prefers-reduced-motion` のときは動かさない。

フォーカスの閉じ込めは `<dialog>` の `showModal()` 任せのままである。

### 動きの終わりで画像が跳ねていた

`<img>` に寸法を指定していなかったため、**表示サイズが「いま読んでいる画像の実寸」で
決まっていた**。先に出すサムネイル（幅 900px）と、あとで差し替える拡大用（幅 2200px）で
枠に収まったときの寸法が変わり、差し替わった瞬間に大きくなる。1920px 幅では
900×600 から 1344×896 へ、1.5 倍に跳ねていた計算になる。

**開くときに、元画像の縦横比から表示サイズを算出して px で固定する。** どちらの画像を
読んでいても寸法が変わらないので、動きの終わりと最終状態が一致する。窓の大きさが
変わったら測り直す。1280px 幅と 1920px 幅で、横位置・縦位置の写真それぞれについて
「動作直後」と「差し替え後」の寸法が一致することを確認した。

## 積み残し（品質）

- Astro 7 が `markdown.remarkPlugins` を非推奨にした。ビルドは通るが警告が出る
  （`remark-neurology-images` の登録先）。`@astrojs/markdown-remark` の
  `unified({...})` へ移す必要がある。
- 神経ノートの一部**壊れ画像リンク**（原コンテンツ由来、404 のまま）。
- 神経ノートの Obsidian 埋め込みで、Notion 由来の `Untitled.png` など**曖昧な参照**は
  解決しきれず 404 になりうる。
- 計算機の実挙動、ダークモードの各ページでの見え方は実機での通し確認が未了。

## 検証状況

- **458ページ静的生成**（2026-08-23。`npm run build:ci` と `npm run lint:text` は緑）。
  内訳の変化は imaging 記事2本の追加による。全ルート HTTP 200 の確認は 453ページ時点のもの。
  2026-08-31 の本番ビルドは 455ページ（`npm run build`。下書き3本を除いた数）。
- 全コンテンツが自己完結（外部パス参照ゼロ）。**`/anatomical-atlas` は 2026-08-23 に
  一度この性質を破っていた**（3D ビューアが three.js を CDN から読んでいた）。
  three.js を同梱して回復。CDN を遮断したヘッドレス Chrome で描画を確認済み。
- **`/anatomical-atlas` の 3D ビューアはヘッドレス Chrome で描画確認済み**
  （`dist` を配信し、iframe 内で単体表示と同じ絵になること）。
  **2026-08-24 にスマートフォン対応版へ差し替えた**（`c6c1673`）が、**実機では見ていない**。
  ダークモードも未確認。
- **ブラウザでの通し確認（レイアウト・ダークモード・計算機の操作）は未了。**
  2026-08-31 に Vercel へ載せたので、実機で見る手立てはできた（要 Vercel ログイン）。
- **`imaging` の2記事はブラウザで一度も見ていない。** 特に引用マーカーの見た目
  （`font-size: 0.78em` / `vertical-align: 0.35em`）は当てずっぽうの値で、実機未確認。
  `npm run dev` → `http://localhost:4321/imaging/what-bold-measures`。
- **記事の数値は PubMed の抄録で原著と一致を確認済み**（Fox & Raichle の +29%/+5%、
  Kwong の 1.8±0.8% と 4.4±2.2秒、Ogawa 1990 がラットであること）。
  ただし **Drew 2019（動脈の1秒以内の能動拡張と静脈の受動拡張）は原著を読んでいない。**
