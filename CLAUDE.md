# tonkura-blog-astro

神経内科・神経画像のノートとブログ。Next.js 版 `tonkura-blog` からの移行版で、まだ本番には出ていない。
移行の判断と切替時の未決事項は `DECISIONS.md` にある。**作業を始める前に読むこと。**

## 開発

```bash
npm run dev        # astro dev
npm run build      # 本番ビルド（draft は出ない）
npm run build:ci   # INCLUDE_DRAFTS=1。下書きも描画する。検証はこちらを使う
npm run lint:text  # textlint（src/content/imaging のみ）
```

Astro 7 / content collections。**スキーマは `src/content.config.ts`**（`src/content/config.ts` は
Astro 4 時代のレガシーパスで、こちらは読まれない。作らないこと）。

## コンテンツの構成

| コレクション | 中身 | 誰が書くか |
|---|---|---|
| `blog` | 個人ノート（6本） | 人 |
| `wiki` | 高次脳機能部の勉強会で出た問い | 人 |
| `database` | 高次脳機能症候の事典 | 人 |
| `neurology` | 神経内科ノート（319本。**Obsidian はもう使わない**） | 人 |
| `imaging` | **神経画像解析の方法論解説。Issue の疑問 1 つ = 記事 1 本** | Claude が下書きし、人が検証して merge |

**読者から見た入口は `/notes` に統一してある。** `wiki` / `neurology` / `blog` / `imaging` は
コレクションとしては別のまま（URL もスキーマも CI もそれぞれ）だが、ナビには Notes の
1つしか出さない。`/notes` が4つを日付順に並べ、種別で絞り込める。構造（問いの繋がり、
319本の階層、読む順）は各コレクションの索引が持っている。**ナビに4つを戻さないこと。**

種別の表示名は Wiki / Neurology / Blog / Imaging である。`neurology` を Notes と呼ぶと
入口の名前とぶつかるので、こちらは Neurology と出す。

### `public/brain-viewer/` は別リポジトリの生成物

`/anatomical-atlas` の 3D ビューア（24MB）は `~/dev/3D_Brain` が作っている。
**ここでは編集しない。** 直したいときは向こうの `viewer/` を直し、
`~/dev/3D_Brain/tools/sync_viewer.sh` で撒く。手でコピーすると、
リポジトリ・配布パッケージ・ここの 3 か所のどれかが必ず古くなる。

three.js は `vendor/` に同梱してある。**CDN から読む形に戻さないこと**
（CDN に届かないとき、進捗 0% のまま無言で止まる）。

**画像は WebP で置く。** `public/neurology-media/` は 2026-08-31 に PNG から変換した
（67.3MB → 9.5MB）。原稿の参照も `.webp` に揃えてある。新しい図を足すときは変換してから置く。

**公開しないページは `src/pages/_archive/` に置く。** 先頭が `_` のディレクトリは Astro が
ルーティングしない。脳画像ツールの旧ページ（`atlas` ほか4つ）がここにある。
tsconfig の `exclude` に入れてあるので `astro check` の対象からも外れる。

## ファイル名の規約（全コレクション共通）

**`[a-z0-9-]+.md`。英小文字・数字・ハイフンだけを使う。**

- **日本語を使わない。** URL に出ると percent-encoding されて化ける
- **空白・括弧・アンダースコアを使わない。** 区切りは `-` に統一する
- **大文字を使わない。** Astro が id を小文字化するので、見た目と実物がずれる
- **日付を名前に入れない。** 順序は frontmatter（`date` / `pubDate`）が持っている
- **分類はディレクトリで表し、ファイル名に入れない。** 分類し直すと URL が壊れる

**先頭の並び順番号は URL に出さない。** リポジトリのファイル一覧で意図した順に並べたいので
番号は付ける。ただし `content.config.ts` の `generateId` が先頭の `\d+[-_]` を落とすため、
URL には出ない。**番号を振り直しても URL と `related` は壊れない。**

| コレクション | ファイル名 | URL |
|---|---|---|
| `imaging` | `01-mri-signal-basics.md` | `/imaging/mri-signal-basics` |
| `blog` | `20250321-tau-pet.md` | `/blog/tau-pet` |
| `wiki` | `q-001-time-vs-speed-perception.md` | `/wiki/q-001`（frontmatter の `id`） |
| `database` | `attention/extinction.md` | `/database/attention/extinction` |
| `neurology` | `diseases/differentials/hypophysitis.md` | 同じパス |

`imaging` は読む順（上流の記事ほど若い番号）、`blog` は日付。`database` と `neurology` は
順序に意味がないので番号なし。`neurology` の表示順は各 `order.json` が持つ。

**ファイル名 = URL、`title` / `label` = 表示名、と役割が分かれている。**
`neurology` は日本語の表示名を frontmatter の `title:` と各 `order.json` の `label:` に持つ。
**ファイル名から表示名を作るコードを書かないこと。** 書くと 319 本の日本語見出しが消える。
経緯は `DECISIONS.md`。

以下は `imaging` を書くときの作法である。他のコレクションには適用しない。

## 読者

神経画像解析をやっている（あるいはこれから始める）研究者。
Python と Linux は使えるが、その手法については初心者。

## 文体

- 日本語。**だ・である調**（textlint が混在を落とす）。
- 専門用語は初出で英語を併記する：安静時機能的結合（resting-state functional connectivity, rsFC）
- 「〜と言われています」を使わない。誰がそう言ったのか出典を出す。
- 一人称は「筆者」。

## 構成

1. **`## 3行まとめ`。** その疑問に3行で答えるならこう、という箇条書きを一番頭に置く。
   ここだけ読んで用が足りる状態にする。前置きも、次節への繋ぎの一文も書かない。
2. **基礎から順に積み上げる本文。** 用語を定義しながら、論理の順に並べる。
   結論から逆算した順ではなく、読者が知らない状態から辿れる順にする。
3. **実務や解釈で効いてくる話。** ただし `## 実務上の注意` のような定型の見出しを付けない。
   記事は疑問への答えであって、書式の埋め合わせではない。その節が実際に何を言っているかで
   見出しを付ける（例: `## BOLD から言えること、言えないこと`、`## GE 法は乱れの出どころを選べない`）。
   **書くことが無ければ節ごと作らない。**

**字数の上限は設けない。理解できることを優先する。**（2026-08-22 決定）
以前は 1500〜3000字を目安にしていたが、これを外した。分量を守るために説明を省くと、
読者が用語を理解できないまま次に進むことになり、記事の目的そのものが立たなくなる。

**読者が知らない用語を、説明せずに使わない。** 「常磁性」「T2\*」「線形時不変系」のような
語を定義なしで出さない。前提が要るなら前提から書く。節が増えても、字数が伸びてもよい。

**淡々と書く。** 「ここで直感に反することが起きる」「言い換えると」「では次に」のような
語りの接続を入れない。事実を順に置く。読者を導こうとしないこと。

## リズムと型（AI 臭を抜くための実測に基づく規則）

Claude が書いた記事と筆者の文章を実測して、差が出た2点だけを規則にする。
一般に言われる「AI っぽさ」の指標（中身のない形容詞・機械的な接続詞・過剰なヘッジ）は
既に他の規則で潰れており、実測でも差が出なかった。

**文の長さを揃えない。** これが最大の差だった。Claude は 21〜35字に35%が集中し、
120字超はゼロ。筆者は6%が120字を超え、標準偏差は倍ある（19.6 対 38.5）。
短い文だけで段落を埋めない。**論理が繋がる文は繋いで長く書く。**
1つの段落の中に、短い文と 80字を超える文を混ぜる。

**用語を定義する型を1つに固定しない。** `〜を…（English）と呼ぶ` が122文中10文（8%）あり、
うち「これを〜と呼ぶ」が4回近接していた。次を混ぜる。

- 同格で置く: 「横緩和時間 T2、すなわち位相の揃いが崩れる速さの指標である」
- 文中の括弧に落とす: 「常磁性（外部磁場と同じ向きに磁化する性質）を持つため」
- 定義せず文脈で示す: 先に現象を書き、後から名前を与える
- 主語にする: 「位相分散とは、揃っていた回転がばらけていく過程を指す」

**禁止しないもの。** 次は英語圏で「AI の癖」と名指しされるが、実測で**筆者のほうが
多用していた**ため禁止しない。避けようとすると筆者の声から遠ざかる。

- 否定対比（「〜ではなく、〜である」）— 筆者の wiki は12%、Claude は6%
- 「〜と考えられる」— 不確かさを示すために意図的に使う（`.textlintrc.json` 参照）

**「この疑問が生じる場面」という節は作らない。** 蛇足になる。疑問そのものは Issue にあり、
記事は答えから始めてよい。

見出しは固定していない。`## 仕組み` のような定型ではなく、その記事の論理に合わせて付ける。
疑問形でもよい（textlint の `no-exclamation-question-mark` はそのために無効化してある）。
**ただし `出典` と `関連する記事` は使わない** — ページ側が自動で出す H2 と重複する。
`実務上の注意` `まとめ` `はじめに` のような、どの記事にも置ける定型の見出しも使わない。

**出典は論文と同じく番号で引く。** 書誌は frontmatter の `sources` に入れ、ページ下部に
番号付きで自動描画される。本文からはその番号を指す。

    ...組織の代謝率の増加は平均 5% にとどまった[3](#ref-3)。

`sources` の並び順が番号であり、**本文での初出順に並べる**。`（Ogawa 1990）` のような
著者名・年での引用マーカーは使わない。角括弧はページ側の CSS が付けるので、本文には
`[3](#ref-3)` と書く。

## 禁止

- **絵文字。**
- **出典のない数値・閾値・パラメータ推奨値。**
- 論文の要旨を読んだだけで結論を断定すること。一次資料（公式ドキュメント or 原著）を最低1件は当たる。
- **体験談の捏造。** 実際にやっていないことを「筆者の環境では〜だった」と書かない。
- 未検証の推測を断定形で書くこと。不確かなら「〜と考えられるが、一次資料で確認が必要」と明示する。
- **著作物の転載。** 論文本文・公式ドキュメントの原文を訳して貼らない。引用は必要最小限にとどめ、
  自分の言葉で説明し直す。図表を複製しない。
- **患者情報。このリポジトリは public である。** 症例・画像・所見を書かない。扱うのは方法論の一般解説だけで、
  筆者自身の研究内容も書かない。

## 機械が落とすもの（プロンプトで頑張らなくてよい）

`npm run build:ci` と `npm run lint:text` が CI で走る。次は自動で落ちる：

- `sources` が2件未満
- `sources` に `kind: docs` か `kind: paper` が1件もない（＝一次資料に当たっていない）
- `title` が8〜60字の外、`description` が40〜160字の外
- `tags` が2〜6個の外
- `related` に存在しない記事の slug がある
- だ・である調とですます調の混在、1文120字超、読点5つ以上

## 記事ファイルの置き方

`src/content/imaging/{並び順2桁}-{英語スラッグ}.md`（例: `03-fmriprep-confounds.md`）。フラット配置。
番号は**読む順**であり、上流の記事ほど若くする。**Issue 番号ではない。**

**category も Issue 番号もパスに含めない** — どちらも後から変わり、URL と `related` を壊すため。
先頭の並び順番号は `generateId` が URL から落とすので、振り直しは安全である。

**全ての記事は Issue を通す。** 対応は frontmatter の `fromIssue` が持ち（必須。CI で落ちる）、
ページ下部に Issue へのリンクが出る。既存記事の分割で生まれた記事も、遡って Issue を立てる。

**読む順は番号で表さない。** Issue 番号は作成順に GitHub が振るもので、記事の依存関係とは
無関係である（実例: MRI 基礎は BOLD の上流だが Issue は #5 と #3）。順序は `related` と
本文中の導線で示す。

`draft` は**必ず `true`** で出す。外すのは人の操作である（merge = 保存、draft を外す = 公開）。
`src/content/imaging/00-template.md` が見本。スキーマの型崩れに気づくために置いてあるので削除しない。
